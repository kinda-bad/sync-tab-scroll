---
plan: plan-f841-2026-07-24-bdce.md
generated: 2026-07-24
status: in-progress
---

# Tasks

## Phase 1: Reproduce

- [ ] T001 [artifacts: ui, datamodel] Write a new Playwright CT spec (`client/src/spotlight-follow.ct.spec.ts`, following `client/src/playback-engine.ct.spec.ts` and `client/src/full-lyrics-view.ct.spec.ts`'s established mount/`__getApi`/`isReadyForPlayback`-polling pattern) that: mounts the same harness `playback-engine.ct.spec.ts` uses to exercise `playback-engine.ts`'s `clientStore.subscribe` callback, waits for `isReadyForPlayback === true` via the existing polling pattern (`full-lyrics-view.ct.spec.ts`'s `expect.poll` usage is the reference), then drives the mounted store/session to `spotlightMode: true` with a fresh `lobbyCursorTick` value, and asserts `api.tickPosition` changes to that value (or, if the harness only exposes it, that the equivalent visible cursor/scroll effect fires). Confirm the test fails against current code before touching any implementation (constitution Principle VII, red-first). Test requirement: this task IS the test — it is expected to be red on completion; a passing result here would mean the reproduction failed to capture the bug and needs revisiting, not that the bug is fixed.
- [ ] T002 Once T001's spec is red, add temporary diagnostic instrumentation (e.g. a `console.log`/assertion probe, removed again before T003) inside `playback-engine.ts`'s `clientStore.subscribe` callback (around the `if (!s.session || !api.isReadyForPlayback) return;` early-return and the Spotlight-follow block at roughly lines 475-517) to determine, from the CT spec's failure, exactly which condition is blocking execution: `api.isReadyForPlayback` staying false, the callback not re-firing on the store update, or the Spotlight-follow `if` condition itself (e.g. `lastAppliedSpotlightTick` already equal to the new tick) never being true. Record the finding — it resolves the plan's first Open Question and determines T003's fix target.

## Phase 2: Fix

- [ ] T003 [artifacts: ui, datamodel] Depends on T002's diagnosis. Fix the actual root cause identified in T002 in `client/src/playback-engine.ts`. If the root cause is local to the Spotlight-follow block itself (e.g. a stale `lastAppliedSpotlightTick` comparison or a logic error in the `if` condition around lines 513-517), fix it there. If the root cause is the shared `api.isReadyForPlayback` gate (line 478) never becoming true for this code path, fix it at that shared root only after confirming (by reading the surrounding code and, if needed, alphaTab's `isReadyForPlayback` semantics) that scenarios 1 and 3 — which share this same gate — are not put at risk; note the confirmation in the commit/PR description. Remove the diagnostic instrumentation added in T002 as part of this task, not left behind. Confirm T001's CT spec now passes.

## Phase 3: Close out

- [ ] T004 Depends on T003. Run the full existing test suite — `pnpm --filter client test` (vitest unit), `pnpm --filter client test:ct` (Playwright CT, including T001's new spec and the existing `playback-engine.ct.spec.ts` suite), and `pnpm --filter server test` (vitest) — and confirm everything passes with no new failures. If any test exercising scenario 1 (Spotlight off → no follow) or scenario 3 (auto-reset on Start) exists, confirm it still passes; if none exists yet at the CT layer, add one cheap CT assertion for scenario 1 (Spotlight off ⇒ `api.tickPosition` unchanged after a `lobbyCursorTick` update) alongside T001's spec, reusing the same harness — cheap now that it exists, and it closes the same live-verified-but-previously-uncovered gap T010 found for that scenario too.
- [ ] T005 Depends on T004. Perform one live two-tab manual re-verification of scenario 2, using the same procedure already used for T010 in `tasks-lobby-cursor-modes-0bea.md` (two real browser tabs — host creates a session and picks a song/part, a second tab joins with the code and picks a part, both need a rendered/persistent alphaTab instance; host sets Spotlight mode on via the Settings modal's Session tab, sets a lobby cursor tick, and confirms the second tab's alphaTab cursor/scroll actually moves to match). Do not mark this complete without this live confirmation, not just CT-green — per T010's own instruction not to close it out on code review alone.
- [ ] T006 Depends on T005. In `.project/tasks/tasks-lobby-cursor-modes-0bea.md`, check off T010, appending a note pointing to this plan (`plan-f841-2026-07-24-bdce.md`) and this tasks file, and recording T005's live re-verification result (pass/fail details) using the same evidentiary style as T010's existing 2026-07-24 verification notes.
