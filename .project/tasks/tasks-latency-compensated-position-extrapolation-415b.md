---
plan: plan-latency-compensated-position-extrapolation-2026-07-18-81b7.md
generated: 2026-07-18
status: in-progress
---

# Tasks

## Phase 1: Extract shared tempo lookup

- [x] T001 [artifacts: infrastructure] Create `client/src/tempo-lookup.ts` exporting `TICKS_PER_QUARTER_NOTE` (960), `ticksToMs(ticks, bpm)`, and a new `localTempoAtTick(score: at.model.Score, tick: number): number` — walks `score.masterBars` accumulating each bar's tick range (`masterBar.start` to `masterBar.start + masterBar.calculateDuration()`), tracking tempo from each bar's `tempoAutomations[0].value` when present (carried forward from the previous bar/song-initial `score.tempo` otherwise, same as `lyrics-gap-timing.ts`'s existing walk), and returns the tempo of whichever masterBar contains `tick` (or the last bar's tempo if `tick` is past the end of the score). Write `client/src/tempo-lookup.test.ts` first, using the same `fakeMasterBar`/`fakeScore` fixture pattern already in `client/src/lyrics-gap-timing.test.ts` (fake objects carrying only the fields the module reads — `start`, `timeSignatureNumerator` is not needed here, `tempoAutomations`, `calculateDuration()`), asserting: a tick within a bar with no tempo automation returns the carried-forward tempo; a tick within a bar that has its own automation returns that bar's tempo; a tick past the end of the score returns the last bar's tempo. Write and confirm it fails before implementing (constitution Principle VII).

- [x] T002 [artifacts: infrastructure] Refactor `client/src/lyrics-gap-timing.ts`'s `computeGapTiming()` to import and use `tempo-lookup.ts`'s `TICKS_PER_QUARTER_NOTE`/`ticksToMs`/`localTempoAtTick` instead of its own inlined copies of the same constant/function and its own inlined masterBar tempo-walk — behavior-preserving (the existing bar-duration-accumulation loop that also tracks `targetBarDurationMs`/`targetTimeSignatureNumerator` stays as-is; only the tempo-tracking sub-logic is replaced by calling the extracted helper, or the loop is restructured to compute the same result via `localTempoAtTick` at each bar's start tick — whichever keeps the existing loop's other per-bar bookkeeping, like `targetTimeSignatureNumerator`, correct with the least duplication). Run the full existing `client/src/lyrics-gap-timing.test.ts` suite and confirm it still passes unchanged (no test edits) — this is the constitution's Principle VII test-first shape applied as a regression guard for a behavior-preserving refactor: the pre-existing tests already specify the required behavior, so there is nothing new to write red-first here.

## Phase 2: Latency-compensated extrapolation

- [x] T003 [artifacts: infrastructure] In `client/src/playback-sync.ts`'s `correctDrift()`, add latency-compensated extrapolation before the existing `const drift = Math.abs(api.tickPosition - playbackState.tickPosition)` line (in the non-host branch, after the existing `if (isHost) return null;` guard): compute `elapsedMs = Date.now() - playbackState.serverTimestamp`, look up `localTempoAtTick(api.score, playbackState.tickPosition)` (import from the new `tempo-lookup.ts`), convert `elapsedMs` to ticks via the inverse of `ticksToMs` (`elapsedTicks = (elapsedMs * TICKS_PER_QUARTER_NOTE * tempo) / 60000`), and use `playbackState.tickPosition + elapsedTicks` as the comparison/correction value in place of the raw `playbackState.tickPosition` in both the `drift` computation and the `onApply`/`api.tickPosition =` assignment below it. Leave every other branch (`status === 'running'`/`'stopped'` transitions, the `isHost` early return) untouched. Write a test in `client/src/playback-sync.test.ts` (create it if it doesn't exist, following the fake-score-fixture pattern from `lyrics-gap-timing.test.ts`/`tempo-lookup.test.ts`) first, asserting: given a `playbackState` with a `serverTimestamp` several hundred ms in the past and a known local tempo, `correctDrift()`'s resulting `api.tickPosition` reflects the elapsed-time-projected value (not the raw `playbackState.tickPosition`) when drift against the projected value exceeds the threshold, and that a small enough elapsed time that keeps the projected value within the drift threshold does *not* trigger a correction (verifying the projection is actually applied to the comparison, not just added as dead code). Write and confirm it fails before implementing.
