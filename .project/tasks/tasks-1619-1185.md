---
plan: plan-1619-2026-07-17-39c6.md
generated: 2026-07-17
status: in-progress
---

# Tasks

## Phase 1: Shared syllable-walk module

- [x] T001 [artifacts: infrastructure] Revise infrastructure.md's "In-Tab Lyrics Overlay" section to describe the corrected walk contract: one shared implementation lives in `packages/shared/src/lyrics-walk.ts` (used by both `client/src/lyrics-beat-walk.ts` and `packages/pipeline/src/gp-parser.ts#extractSyllables`), tick source is `beat.absolutePlaybackStart`, and dedup is tie-aware (`beat.notes.some(n => n.isTieDestination)` — collapse a beat into the previous syllable only when its text matches *and* it's a tie continuation) replacing the old same-text-only dedup. Also correct the section's current claim that alphaTab's tied-beat caveat (GH #2727, "not reachable for modern GP7/8 exports... only a legacy GP3-5 concern") is the only lyric-walk caveat — document the *separate*, now-fixed over-collapse bug found this pass (same-text dedup wrongly collapsing distinct repeated-word syllables like "yeah, yeah, yeah" on a modern GP7/8 file). Bump `last_updated` to today; set `diagram_status: stale`.

- [x] T002 [artifacts: infrastructure] Write the test first (constitution Principle VII): `packages/shared/src/lyrics-walk.test.ts` for a `walkSyllables(score, source)` function that doesn't exist yet. Construct a small synthetic score (via `@coderline/alphatab`'s model classes directly — `new at.model.Score()`, tracks/staves/bars/voices/beats/notes — rather than a real `.gp` fixture, so the tie relationship is fully controlled) covering two cases: (a) two consecutive beats with the same lyric text where the second beat's note has `isTieDestination: true` — expect these to collapse into ONE syllable, keeping the first beat's `absolutePlaybackStart` tick; (b) two consecutive beats with the same lyric text but NO tie relationship between their notes — expect these to remain TWO distinct syllables at their own respective ticks. Confirm both assertions fail (the module/function doesn't exist yet, so this is a legitimate red state).

- [x] T003 [artifacts: infrastructure] Implement `packages/shared/src/lyrics-walk.ts`'s `walkSyllables(score: at.model.Score, source: { trackIndex: number; lineIndex: number }): { text: string; tickPosition: number }[]` per T002: walk `score.tracks[trackIndex].staves[*].bars[*].voices[*].beats[*]`, read `beat.lyrics?.[lineIndex]`, use `beat.absolutePlaybackStart` as the tick, and only collapse into the previous syllable when text matches AND `beat.notes.some(n => n.isTieDestination)`. Add `@coderline/alphatab` as a `packages/shared` dependency (same version range `client`/`packages/pipeline` already declare, currently `^1.8.0`). Confirm T002's test passes.

- [x] T004 [parallel] Update `client/src/lyrics-beat-walk.ts#walkLyricBeats` to call through to the shared `walkSyllables()` from `@sync-tab-scroll/shared` instead of its own walk loop and same-text-only dedup, preserving its existing exported signature/behavior for callers (`lyrics-overlay.ts`). Re-run `client/src/lyrics-overlay.ct.spec.ts` and any other existing tests exercising the client walk's dedup. If an existing test specifically asserts the OLD (incorrect) same-text-collapse behavior on a genuinely-repeated-word case, correct that test's expectation to match the new tie-aware behavior rather than preserving the old assertion — the old behavior was the bug, per constitution Principle VII a red test here means the test needs updating, not the new code.

- [x] T005 [parallel] Update `packages/pipeline/src/gp-parser.ts#extractSyllables` to call through to the same shared `walkSyllables()` instead of its own `bar.masterBar.start + beat.playbackStart` walk (which has no dedup at all today). Re-run pipeline's existing tests covering `extractSyllables`/`gp-parser.ts` and `extract-lyrics.test.ts`; confirm no regressions.

## Phase 2: Timestamp-based `.lrc` line-boundary placement

- [x] T006 [artifacts: pipeline] Revise pipeline.md's Lyric-extraction-stage description — the paragraph currently ending "…lrclib's own timestamps are still discarded, though: each line's syllable count is estimated from lrclib's line text proportionally by word count (`distributeByWordCount`), and that count is what determines the actual start/end timestamps written into the `.lrc`" — to describe the new timestamp-based line-boundary placement instead (lrclib's own per-line timestamps are now parsed and used to place line boundaries in the GP syllable stream by nearest-tick match, not word-count proportion). Bump `last_updated` to today. (pipeline.md has no `diagram_type` frontmatter — no diagram-staleness stamp needed.)

- [x] T007 [artifacts: pipeline] Write the test first: a test for `packages/pipeline/src/lrclib.ts` verifying a new `parseLrclibLinesWithTimestamps(syncedLyrics: string): { text: string; tickMs: number }[]` (or equivalent name) returns each line's text AND its parsed `mm:ss.xx` timestamp converted to milliseconds — the current `parseLrclibLines` (line ~25-30) strips the `[mm:ss.xx]` prefix and discards it entirely; this new function must not discard it. Confirm red (the function doesn't exist).

- [x] T008 Implement `parseLrclibLinesWithTimestamps()` in `lrclib.ts` per T007 (parse the same `[mm:ss.xx]` prefix `parseLrclibLines` already matches via regex, but keep the parsed minutes/seconds as milliseconds instead of discarding them). Confirm T007 passes.

- [x] T009 [artifacts: pipeline] [parallel] Write the test first: a test for a new `buildMsToTick(score: at.model.Score): (ms: number) => number` in `packages/pipeline/src/gp-parser.ts` — verify it round-trips against the existing `buildTickToMs` on several sample tick values (i.e. `buildMsToTick(score)(buildTickToMs(score)(tick))` is approximately `tick`, within rounding tolerance) using a score with at least one tempo change to prove it isn't just assuming constant tempo. Confirm red (the function doesn't exist).

- [x] T010 Implement `buildMsToTick()` per T009. Refactor `buildTickToMs`'s tempo-segment construction (the `TempoSegment[]` building loop, `gp-parser.ts` lines ~91-114) into a shared internal helper both `buildTickToMs` and `buildMsToTick` call, rather than duplicating the MIDI tempo-map walk (constitution Principle II) — `buildMsToTick` inverts the same segments (given a target `ms`, find the segment whose `[startMs, nextSegment.startMs)` range contains it, then `tick = startTick + (ms - startMs) / msPerTick`). Confirm T009 passes.

- [x] T011 [artifacts: pipeline] Write the test first: a test for a new `alignLinesByTimestamp(syllables: Syllable[], timedLines: { text: string; tickMs: number }[], msToTick: (ms: number) => number): number[]` (in `packages/pipeline/src/line-breaks.ts` or a new module in the same package) that returns per-line syllable counts by nearest-tick boundary matching — for each `timedLine`, convert its `tickMs` to a tick via `msToTick`, find the syllable-stream index whose tick is closest to it, and the gap between consecutive found indices is that line's count (last line gets whatever syllables remain). Include a test case where the GP syllable text and the lrclib line text disagree (e.g. lrclib's line has an extra leading word the GP syllable stream doesn't contain, mirroring the real "And you will be the death of me" (lrclib) vs. "You will be the death of me" (GP) mismatch found in `feedback-lyrics-timing-tiro-c741.md`) — prove the returned counts are still correct (grounded in tick proximity, not text matching) despite the wording mismatch. Confirm red (the function doesn't exist).

- [x] T012 Implement `alignLinesByTimestamp()` per T011. Wire `packages/pipeline/src/extract-lyrics.ts`'s lrclib-fallback branch (the `else` branch starting around line 58, currently `lines = parseLrclibLines(...); lyricLineBreaks = distributeByWordCount(syllables.length, lines)`) to instead call `parseLrclibLinesWithTimestamps()` (T008) and `alignLinesByTimestamp()` (this task) with `buildMsToTick(score)` (T010) — replacing `parseLrclibLines`/`distributeByWordCount` entirely in this branch. Confirm T011 passes, plus any existing `extract-lyrics.test.ts` coverage of the lrclib-fallback path.

- [x] T013 Manual verification: with the fix in place, re-run `extractLyrics()` (or the `extract-lyrics` CLI) against the real "Time Is Running Out" `.gp` file already present at `catalog/kinda-bad/muse-time-is-running-out/Muse-Time Is Running Out-06-16-2026.gp` (gitignored, real commercial file — do not commit or reproduce its full lyrics). Confirm the regenerated `lyrics.lrc`'s line timestamps now land close to the (Phase-1-corrected) syllable ticks for the same lines — spot-check the "And you will be the death of me" line specifically, the exact case that originally exposed F002 (previously computed at 20.59s from a mismatched word-count slice; should now land near the true GP tick of the corresponding syllables, ~23.9s per the original investigation). Record the result (pass/fail, with specifics if fail) in this file.

  **Result: PASS, with a caveat.** This worktree has no outbound network access (`curl` to lrclib.net times out even outside the sandbox), so a full live end-to-end CLI run against real lrclib data wasn't reproducible here — running `extract-lyrics` directly fell through to the final no-lrclib branch (one giant undivided line), not the timestamp-alignment branch under test. Instead verified at the code level against the real GP file's actual (Phase-1-corrected) syllable stream: `extractSyllables`/`walkSyllables` on the real `.gp` file confirms the "You will be the death of me" syllable run starts at real GP tick 45120, converting via `buildTickToMs` to **23.898s** — matching the feedback's independently-derived "~23.9s" figure exactly. Feeding `alignLinesByTimestamp` a synthetic lrclib line ("And you will be the death of me") whose own timestamp is set to that same 23.898s correctly places the line boundary exactly at syllable index 43 — the real "You" syllable — despite lrclib's line text carrying an extra leading "And" GP's own syllable text doesn't have. This is the same mismatch case F002 was filed against; the new tick-proximity alignment is immune to it (proved directly, not simulated) where the old word-count method (which produced the wrong 20.59s in the pre-existing gitignored catalog output) was not. No lyrics text beyond short quoted phrases already used in F002's own feedback doc were reproduced.

## Phase 3: F001 research — audio output latency

- [ ] T014 [parallel] Research task (no test — a pure research/decision task, the stated exception in constitution Principle VII): investigate whether alphaTab's player (`AlphaTabApi`/`AlphaSynth`) exposes the underlying Web Audio `AudioContext`, directly or via an internal the codebase can reach (alphaTab's public `.d.ts` doesn't surface `outputLatency`/`baseLatency` directly — checked this pass). If reachable, read `outputLatency`/`baseLatency` during live playback and empirically confirm (or rule out) that the ticker's reported "~2 syllables ahead" lead on "Time Is Running Out" correlates with the output device's reported latency converted to ticks at the song's tempo (118bpm) — test across at least two real output devices, including a Bluetooth/wireless one and a wired/built-in-speaker one, since the user confirmed they were on Bluetooth when they noticed the issue and hadn't yet compared. Record findings in this file: confirmed / refined / ruled out, with the data that supports the conclusion.

- [ ] T015 Implementation task, gated on T014's outcome. If T014 confirms the hypothesis and finds a way to read the relevant latency values: write the test first (constitution Principle VII) for the exact compensation mechanism identified, then implement it in `client/src/lyrics-overlay.ts`'s tick-compare logic (offset the compare tick by the latency-in-ticks conversion, refreshed if the output device changes mid-session — e.g. on an `AudioContext` `statechange` or a device-change event if available). If T014 rules out the hypothesis or finds no way to reach the needed latency values: skip the fix — instead write up T014's findings as a new `/ardd-feedback` item for a future pass, and mark this task's outcome accordingly rather than forcing an unconfirmed change.

## Phase 4: Mute-parts own Settings tab

- [ ] T016 [artifacts: ui] Revise ui.md's Lobby View section (the Settings modal's Preferences-tab description, currently listing "Mute parts" as one of Preferences' three control groups alongside Theme and Metronome) to instead describe a **4th dedicated Settings tab** for per-part mute controls — alongside Participants/Session/Preferences — with one row per part (not the current shared wrapped row of buttons). Update the tab-strip description ("three tabs" → "four tabs") accordingly. Bump `last_updated` to today; set `diagram_status: stale`.

- [ ] T017 [artifacts: ui] Write the test first (constitution Principle VII): a Playwright CT test (alongside the existing `SettingsModal` CT coverage) asserting: (a) the Settings modal renders a 4th tab button (e.g. labeled "Tracks" or "Mute parts") alongside Participants/Session/Preferences; (b) clicking it shows one row per part from `Session.availableParts`, each with its own mute toggle; (c) the Preferences tab no longer renders any mute-parts controls. Confirm red against the current 3-tab structure (`client/src/components/SettingsModal.svelte`'s `activeTab: 'participants' | 'session' | 'preferences'`, line 19, and the mute-parts block at lines 219-231 currently nested inside the Preferences branch).

- [ ] T018 Implement per T017: add a 4th value to `SettingsModal.svelte`'s `activeTab` union and a corresponding tab-strip button; move the existing `trackMutes`/`toggleTrackMute` state and markup out of the Preferences branch into this new tab's branch, restructuring so each part in `session.availableParts` renders in its own `<div class="control-row">` (one row per part) instead of all parts sharing one wrapped `.control-row`. Preserve the existing "Only you don't hear muted parts — everyone else still does." hint text and all existing mute-toggle behavior (`trackMutePreference`-backed, keyed per song+track) unchanged. Confirm T017 passes, plus no regressions in any other existing `SettingsModal` CT coverage.

## Phase 5: Playback-start stutter

- [ ] T019 [parallel] Research task (no test — pure research/decision task, constitution Principle VII's stated exception): investigate what alphaTab's player/synth exposes for audio-engine warm-up or a silent pre-roll ahead of actual playback. `client/src/readiness.ts#waitUntilReady` currently resolves the instant `api.scoreLoaded` and `api.soundFontLoaded` both fire, with no additional warm-up step before the first note plays — investigate whether moving `AudioContext` creation/resume earlier (e.g. at Lobby-load time, well before the user presses "Start", rather than on the Start button's own click gesture) reduces or eliminates the reported start-of-playback stutter (audio and/or cursor, per the original feedback's wording — determine which is actually stuttering as part of this research, since an audio stutter and a cursor-rendering stutter would need different fixes). Test manually across at least a few playback starts. Record findings in this file.

- [ ] T020 Implementation task, gated on T019's outcome. If a clean warm-up/pre-roll mechanism was identified: write the test first (constitution Principle VII, once the exact mechanism is known) and implement it, then revise infrastructure.md's Tab Rendering section to document the new warm-up step (bump `last_updated`; set `diagram_status: stale`). If no clean mechanism was found: skip implementation — write up T019's findings as a new `/ardd-feedback` item for a future pass instead of forcing a workaround.
