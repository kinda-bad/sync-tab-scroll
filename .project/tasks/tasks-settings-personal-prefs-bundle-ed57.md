---
plan: plan-settings-personal-prefs-bundle-2026-07-18-bd58.md
generated: 2026-07-18
status: in-progress
---

# Tasks

## Phase 1: Ticker font-size preference

- [x] T001 [artifacts: ui] Create `client/src/lyrics-ticker-font-size-preference.ts`, mirroring `client/src/metronome-preference.ts`'s shape exactly (a `STORAGE_KEY` constant, a `loadStoredLyricsTickerFontSize(): 'small' | 'medium' | 'large' | 'huge'` reading from `localStorage` and defaulting to `'medium'` on an unset/invalid value, and a `persistLyricsTickerFontSize(size)` writer). Write `client/src/lyrics-ticker-font-size-preference.test.ts` first (mirroring `metronome-preference.test.ts`'s structure) asserting: default is `'medium'` when unset, persisted values round-trip through load, and an invalid/corrupt stored value falls back to `'medium'` rather than throwing. Write and confirm it fails before implementing (constitution Principle VII).

- [x] T002 [artifacts: ui] [parallel with T004] Wire the font-size preference into the in-tab lyrics ticker: add a `--lyrics-ticker-font-size` CSS custom property to `client/src/styles/motifs.css`'s `.lyrics-overlay` rule (replacing its hardcoded `font-size: 1.125rem`), with four step values chosen to be clearly, noticeably distinct from their neighbors (not a subtle scale) — e.g. roughly 0.85rem/1.125rem/1.5rem/2rem, adjusted by visual judgment at implementation time. In `client/src/lyrics-overlay.ts`, set that custom property on the overlay element from `loadStoredLyricsTickerFontSize()` at creation, and expose a way to update it live (e.g. a `setFontSize` method on the returned `LyricsOverlay` interface). Add a "Lyrics ticker font size" control (four options: small/medium/large/huge) to `client/src/components/SettingsModal.svelte`'s Preferences tab, following the existing Metronome toggle's control-row pattern, calling `persistLyricsTickerFontSize` and the overlay's live-update on change. Write a Playwright CT spec asserting: the control renders in the Preferences tab with all four options, selecting a size persists it (verify via `localStorage` in the test) and updates the overlay's computed font-size live. Write and confirm it fails before implementing.

## Phase 2: Measure markers

- [x] T003 [artifacts: ui] Extend `client/src/lyrics-overlay.ts` to accept measure-boundary tick data: reuse `client/src/lyrics-gap-timing.ts`'s existing local-tempo/time-signature measure-duration derivation to compute each measure's start tick and number from the loaded score, then insert marker elements (a vertical line + measure-number label, new CSS classes in `client/src/styles/motifs.css`) into the `.lyrics-track`'s child sequence at the correct tick-sorted position relative to the syllable spans — mirroring the existing `gap-dot`/`gap-drain` insertion pattern in `client/src/playback-engine.ts`'s full lyric sheet rendering, applied to the ticker instead. Critically, `activeIndex`/`updateActiveSyllable()`/`centerActiveSyllable()` must keep indexing only the real syllable array (`flat`) — markers are inserted into the DOM without being added to `flat`, so they never shift syllable-highlight/centering logic. Write a Playwright CT spec first asserting: given a score with known measure boundaries, marker elements appear at the expected positions in the DOM sequence relative to syllable spans, with correct measure numbers, and that syllable highlighting/centering (`at-highlight`, `centerActiveSyllable`) is unaffected by their presence. Write and confirm it fails before implementing.

- [x] T004 [artifacts: ui] [parallel with T002] Create a small preference module for the "Measure markers" toggle (same load/persist shape as T001's `lyrics-ticker-font-size-preference.ts`, but boolean, default off — e.g. `client/src/lyrics-measure-markers-preference.ts`). Wire it into `client/src/components/SettingsModal.svelte`'s Preferences tab as a toggle (following the existing Metronome toggle's pattern) and into `lyrics-overlay.ts`'s marker rendering from T003 (markers render only when the preference is on; toggling live shows/hides them without recreating the overlay). Write a test for the preference module first (mirroring T001's test shape) and a Playwright CT spec asserting the toggle persists and controls marker visibility live. Write and confirm both fail before implementing.

## Phase 3: Per-part Solo button

- [x] T005 [artifacts: ui] [parallel with T001, T003] Add a "Solo" button to each part row in `client/src/components/SettingsModal.svelte`'s Tracks tab, alongside the existing per-part mute toggle. Clicking "Solo" for part *P* calls `api.changeTrackMute()` (the same call the individual mute toggles already use) to mute every other part in `Session.availableParts` except *P*, persisting the resulting mute state per part via the existing `client/src/track-mute-preference.ts` (same song+track keying as today) — no new "solo" persisted concept; it's an ordinary batch mute-state change, not a tracked mode. Write a Playwright CT spec first asserting: clicking "Solo" on a part mutes every other part (verify via `api.changeTrackMute()` calls or resulting mute-toggle states) while leaving the soloed part unmuted, and that the resulting state persists through `track-mute-preference.ts` like any other mute change. Write and confirm it fails before implementing.
