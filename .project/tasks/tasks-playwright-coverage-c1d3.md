---
plan: plan-playwright-coverage-2026-07-02.md
generated: 2026-07-02
status: ready
---

# Tasks

## Phase 1: Playwright infrastructure

- [ ] T001 Install `@playwright/test` and `@playwright/experimental-ct-svelte` as `client` dev dependencies (`pnpm --filter client add -D @playwright/test @playwright/experimental-ct-svelte`). Run `pnpm --filter client exec playwright install chromium` (Chromium only — no cross-browser matrix requested or justified for this project's scale).
- [ ] T002 Write `client/playwright.config.ts` defining two projects: `e2e` (testDir `./e2e`, a `webServer` array config launching the real `server` package with `CATALOG_ROOT` pointed at `../catalog` on one port, and `vite preview` serving a production `client` build on another port — matching what a real user gets, not the dev server) and `ct` (using `@playwright/experimental-ct-svelte`'s component-testing config, testDir `./src` matching `*.ct.spec.ts` colocated with source, mirroring the vitest `*.test.ts` colocation convention). Confirm `pnpm --filter client exec playwright test --list` enumerates both projects (even with zero test files yet) without config errors.
- [ ] T003 Add `client/package.json` scripts: `"test:e2e": "playwright test --project=e2e"` and `"test:ct": "playwright test --project=ct"`. Leave the existing `"test"` (vitest) script untouched — this is a second, distinct test command, not a replacement.

## Phase 2: ws-client component test

- [ ] T004 Write `client/src/ws-client.ct.spec.ts`: in a `beforeEach`, start a minimal local WebSocket server (plain `ws` package — a few lines, not the full `server` package) on an ephemeral port; tear it down in `afterEach`. Mount/exercise `client/src/ws-client.ts` against it (read the file first to confirm its actual public shape — constructor/connect signature, `send()`, and however it dispatches incoming messages to subscribers — since this task's assertions depend on that exact interface). Confirm: `send()` transmits the expected JSON-encoded message shape to the test server; a message sent from the test server arrives at whatever subscriber/callback mechanism `ws-client.ts` exposes, with the parsed payload intact. Run `pnpm --filter client test:ct` and confirm it passes.

## Phase 3: alphaTab-coupled component tests [parallel across files]

- [ ] T005 [parallel] Write `client/src/tab-renderer.ct.spec.ts`: mount a harness component that calls `createTabRenderer` (`client/src/tab-renderer.ts`) against the real `catalog/radiohead-creep/Radiohead-Creep-01-01-2026.gp` fixture (served as a static asset reachable by the CT dev server — check how Playwright CT serves fixture files and place/reference the `.gp` file accordingly, e.g. via a `public`/fixtures directory Playwright CT can serve). Confirm real SVG output appears inside the container element once the score loads (poll/wait on `scoreLoaded`, not a fixed sleep). Confirm `setTheme(api, 'light')` after a dark-mode render visibly changes at least one rendered element's color (e.g. inspect a `<text>` or path's `fill` via the DOM) versus the dark-mode render. No audio assertions. Run `pnpm --filter client test:ct` and confirm it passes.
- [ ] T006 [parallel] Write `client/src/headless-player.ct.spec.ts`: mount/exercise `createHeadlessPlayer` (`client/src/headless-player.ts`) against the same fixture `.gp` file, with no visible container. Confirm the returned `api` reaches `isReadyForPlayback: true` (or fires whatever readiness event the module exposes — read the file first) without ever attaching to a DOM container. No audio assertions. Run `pnpm --filter client test:ct` and confirm it passes.
- [ ] T007 [parallel] Write `client/src/lyrics-overlay.ct.spec.ts`: mount `createLyricsOverlay` (`client/src/lyrics-overlay.ts`) with a small fixture set of lyric lines/syllables (plain fixture data, not a real score) and a container element. Drive the tick position forward past each line's timestamp and confirm the overlay's rendered DOM text updates to the expected active line at each step. Run `pnpm --filter client test:ct` and confirm it passes.
- [ ] T008 [parallel] Write `client/src/playback-engine.ct.spec.ts`: mount `ensurePlaybackEngine` (`client/src/playback-engine.ts`) against a real container and the fixture `.gp` file, using a `ws-client` test double (a plain object matching `WsClient`'s real public shape from `client/src/ws-client.ts` — read it first — recording every `send()` call, with no real network connection) in place of a real `WsClient`. Feed synthetic `clientStore` updates (via the real `clientStore` from `client/src/store.ts`, calling `.set()`) and confirm, against the real `api` instance: (a) drift correction applies when `playbackState.tickPosition` diverges from `api.tickPosition` beyond the documented threshold; (b) Spotlight-mode force-follow — `api.tickPosition` snaps to `session.lobbyCursorTick` only when `session.spotlightMode` is `true`, and does not move when `spotlightMode` is `false` (this is the real-DOM/real-alphaTab counterpart to `playback-sync.test.ts`'s pure-function-level coverage from `tasks-test-coverage-bfe8.md`); (c) `api.settings.player.enableUserInteraction` toggles based on host-only, paused-only seek eligibility, matching `ui.md`'s documented behavior. No audio assertions. Run `pnpm --filter client test:ct` and confirm it passes.
- [ ] T009 [parallel] Write `client/src/store.ct.spec.ts`: mount a small harness Svelte component that subscribes to `clientStore` and renders a piece of its state (e.g. `view` or `session?.code`). Confirm the mounted component's rendered DOM updates after calling `clientStore.set(...)`/`clientStore.update(...)` with new values. Run `pnpm --filter client test:ct` and confirm it passes.

## Phase 4: End-to-end flows [parallel across files]

- [ ] T010 [parallel] Write `client/e2e/single-participant.spec.ts`: using the `e2e` project's real client+server+`catalog/radiohead-creep` fixture, drive one browser context through Landing (create a session) → Lobby (select the `radiohead-creep` song, select an instrument part) → confirm readiness reaches `ready` → host clicks Start → Playback view shows a rendered tab canvas element. Repeat the flow selecting the Lyrics part instead, confirming the full-lyrics view renders instead of a tab canvas. No audio assertions — assert on DOM/visual presence and `session.playbackState.status` only.
- [ ] T011 [parallel] Write `client/e2e/multi-participant.spec.ts`: two Playwright browser contexts (host + member) joined to the same session via its code. Confirm song/part selections made by one participant are reflected in the other's Lobby view. Cover the Spotlight-mode scenario that `tasks-lobby-cursor-modes-0bea.md`'s `T010` left blocked on manual verification: with Spotlight mode **off**, the host setting the lobby cursor does not move the member's view; toggled **on**, it does; Start then Stop confirms both the lobby cursor and Spotlight mode auto-reset to their defaults. This test is intended to make the blocked manual `T010` unnecessary going forward — note that in this task's completion note.
- [ ] T012 [parallel] Write `client/e2e/host-controls.spec.ts`: host Start/Pause/Resume/Stop transitions are reflected in `session.playbackState.status` visible to both host and a joined member; host removing a participant results in that participant disappearing from the other's participant list.

## Phase 5: Full suite verification

- [ ] T013 Run `pnpm --filter client test`, `pnpm --filter client test:ct`, `pnpm --filter client test:e2e`, and `pnpm --filter server test` — confirm no port collisions between the `e2e` project's `webServer` processes and any already-running dev server on the same ports (adjust `playwright.config.ts`'s ports if a collision is found), no flaky failures across 3 consecutive full runs of `test:ct`/`test:e2e`, and no Chrome-autoplay-policy errors leaking into test output (confirming Phase 1-4's "no audio assertions" scoping actually held in practice, not just in intent). Document the full command set and final test/file counts across all four suites in this task's completion note.
