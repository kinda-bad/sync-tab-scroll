---
plan: plan-ui-polish-pass-2026-07-03.md
generated: 2026-07-03
status: in-progress
---

# Tasks

## Phase 1: Cursor + click-to-seek

- [x] T001 Confirm the `enablePlayer` hypothesis before changing code: in a real browser (dev server), after a tab renders, inspect `api.settings.player.playerMode` in the console. If it's not `PlayerMode.EnabledSynthesizer` (or `EnabledAutomatic`) despite `enablePlayer = true` being set in `client/src/tab-renderer.ts`, that confirms the deprecated property is a no-op in this alphaTab version. Record the actual observed value in this task's completion note either way — this is a confirm-or-refute step, not a fix.

  **Hypothesis disproven.** Checked via `tab-renderer.ct.spec.ts`'s real alphaTab harness (reliable in this environment, unlike browser automation via the Chrome extension): `enablePlayer = true` already maps to `playerMode: 1` (`PlayerMode.EnabledAutomatic`) — not a no-op. `enableCursor: true` and `enableUserInteraction: true` are also already correctly set. Further check: the cursor DOM elements (`.at-cursors` > `.at-cursor-bar`/`.at-cursor-beat`) genuinely exist in the rendered container with real inline positioning styles (`position: absolute`, non-zero width/height, transforms) immediately after `render()` — this doesn't depend on `isReadyForPlayback` at all. No global CSS reset (`tokens.css`'s only `*` rule is `box-sizing: border-box`) or container `overflow`/`position` rule found that would hide/clip them either. **Root cause is not what the plan hypothesized.** Stopping per the plan's own instruction rather than guessing a second fix — reported back to the user for direction before T002/T003 (which would have "fixed" a setting that was never actually broken).
- [x] T002 [artifacts: brand] Write a test first (Principle VII): add a test to `client/src/tab-renderer.ct.spec.ts` asserting that after `createTabRenderer()` renders, `.at-cursor-bar`, `.at-cursor-beat`, and `.at-selection` each have a non-transparent `getComputedStyle(...).backgroundColor` (i.e. not `rgba(0, 0, 0, 0)`) inside the tab container. This doesn't depend on `isReadyForPlayback`/`soundFontLoaded`, so it isn't blocked by the known CT audio-worklet gate. Confirm it fails against the current code (all three are currently fully transparent — confirmed via subagent investigation).

  Confirmed red (all three `rgba(0, 0, 0, 0)`) before the fix. 4/4 `tab-renderer.ct.spec.ts` tests passing after.
- [x] T003 Add CSS styling `.at-cursor-bar`, `.at-cursor-beat`, and `.at-selection` using `--riot`/`--hazard` tokens (wherever the app's tab-container-adjacent styles already live — check `client/src/App.svelte`'s `<style>` block first, alongside `.tab-container`/`.engine-containers`) with opacity tuned to read clearly against `brand-colors.ts`'s `darkTabColors` without obscuring the notation underneath (start from the subagent's illustrative values — `.at-cursor-bar` lower opacity as a highlight band, `.at-cursor-beat` a bit stronger for the active beat, `.at-selection` low opacity — then adjust by eye in T004, these aren't final numbers). Run T002's test and confirm it now passes.

  **Scoping correction found while implementing:** these are alphaTab-injected DOM elements, not part of any Svelte component's own template — a scoped `<style>` block in `App.svelte` (Svelte's default) would never reach them since Svelte's `:global()` scoping is per-component and the CT harness mounts a completely different component tree than the real app. Added the rules to the global stylesheet `client/src/styles/motifs.css` instead (unscoped, `.at-` prefixed classes are alphaTab-namespaced so collision risk is negligible), and updated `client/playwright/index.ts` (the CT mount page, previously loading no global CSS at all) to import `tokens.css`/`motifs.css` — otherwise the CT test could never have observed the fix regardless of where the CSS lived. Values used: `.at-cursor-bar` → `var(--riot-dim)`, `.at-cursor-beat` → `var(--riot)` at `opacity: 0.6`, `.at-selection` → `var(--hazard-dim)`.
- [x] T004 Manual verification in a real browser (dev servers already running or restart per earlier session pattern): start playback on an instrument part and confirm the cursor is now visibly colored and tracks the playing beat, legible against the dark-mode canvas. While paused as host, click on the tab notation and confirm playback seeks to that position, with the now-visible cursor jumping there — this was likely never actually broken (`enableUserInteraction` was already correctly configured per T001's investigation), just impossible to perceive without a visible cursor providing feedback; confirm that's the case rather than assuming it. If seeking is still broken independent of cursor visibility, that's a genuinely separate issue — stop and report rather than guessing further. Tune the CSS opacity/colors from T003 visually if needed.

  **Confirmed via real browser (claude-in-chrome, dev servers on :5180/:8080).** Cursor visibly renders as a riot-pink highlight band + brighter beat marker over the current bar, clearly legible against the dark canvas, both in the pre-playback Lobby preview and during live playback. Click-to-seek while paused: clicking bar 10 immediately jumped the visible cursor there — **confirms the hypothesis exactly**, seeking was never actually broken, it was just imperceptible without a colored cursor providing feedback. Opacity values from T003 read well as-is; no visual tuning needed.

## Phase 2: Lyrics overlay positioning

- [x] T005 Write a test first: add a test to `client/src/lyrics-overlay.ct.spec.ts` asserting the container element has `position: relative` (via `getComputedStyle`) and `.lyrics-overlay` has `position: absolute` once `createLyricsOverlay` runs. Confirm it fails against the current CSS (no positioning rules exist yet).

  Confirmed no `.lyrics-overlay`/`.lyric-syllable`/`.at-highlight` CSS existed anywhere (grepped before writing any fix). Test written asserting `.lyrics-overlay`'s computed position — see T006's note for why the assertion ended up checking `fixed`, not `absolute` as originally planned.
- [x] T006 Add the positioning CSS — `position: relative` on whatever container element hosts the tab canvas + overlay (locate the actual styling location: likely `client/src/App.svelte`'s `.engine-containers`/`.tab-container` rules, not `lyrics-overlay.ts` itself, which only builds DOM nodes, not styles) and `position: absolute` (plus an inset covering the tab area, and a `z-index` above the rendered SVG) on `.lyrics-overlay` in whichever stylesheet currently defines `.lyrics-overlay`/`.lyric-syllable`/`.at-highlight` (check `client/src/styles/` first; if these classes have no existing styles at all, that's itself part of the bug — add them). Run T005's test and confirm it passes.

  **Corrected mid-implementation after live-browser verification caught a real problem:** an initial `position: absolute` (relative to `.engine-containers`, a new `.lyrics-overlay-container` class added to `App.svelte`'s existing overlay div) technically passed the CT test but, live in a real browser, sat far below the fold — `.engine-containers` is as tall as the *entire* rendered score (often several screens), so `bottom: 0` anchored to the bottom of the whole notation, not the visible viewport. Switched to `position: fixed` (viewport-relative, pinned above the persistent Bar via `--bar-height`), which is genuinely "on top of the tab notation" regardless of scroll position. Test updated to match. Added global CSS in `motifs.css` (same reasoning as the cursor rules: `.lyrics-overlay`/`.lyric-syllable` are alphaTab-adjacent plain DOM, not Svelte-templated, so scoped component styles wouldn't reach them). Also updated `client/playwright/index.ts`'s global CSS import (already done for T003) to cover this too.
- [x] T007 Manual verification in a real browser: select an instrument part on a song whose lyrics-bearing track differs from the viewed track, start playback, and confirm the lyrics overlay now renders on top of the tab notation (not stacked below it) and the active-syllable highlight is visible against the notation. If true overlay placement causes visual conflicts with alphaTab's own SVG layering (e.g. the overlay obscuring notation, or z-index fighting), stop and report the specific conflict to the user for a placement discussion, per the feedback's own caveat — do not ship a different placement unannounced.

  **Confirmed via real browser.** Selected Bass (viewed track) on Creep, whose lyrics live on the Vocals track (`lyricsTrackIndex: 0`) — a genuinely different track, matching the scenario this task calls for. During playback, the lyrics ribbon renders as a persistent translucent band pinned above the Bar, staying visible regardless of scroll position through the (long) rendered score — this is what caught the `position: absolute` bug in T006 in the first place. No z-index fighting with alphaTab's SVG observed. Not a different placement than intended — same "bottom caption band" design, just correctly viewport-pinned instead of document-pinned.

## Phase 3: Bar dark-mode fill contrast

- [x] T008 [artifacts: brand] Add a `--bar-surface` custom property to `client/src/styles/tokens.css` for both `data-theme="dark"` and the light theme, distinct from the existing `--surface` value in each (a value visually differentiated from `--surface` while staying within `brand.md`'s established dark/light palettes — do not introduce a new hue family, adjust lightness/tint within the existing palette). Update `client/src/components/Bar.svelte`'s `background: var(--surface)` to `background: var(--bar-surface)`.

  **Found and avoided a real trap:** `--surface-raised` already exists in `tokens.css` but is already spoken for — it's `HazardBar.svelte`'s own base/track color (the "empty" color behind the stripe fill). Reusing it for the Bar's root too would have made the Bar and its own hazard-stripe track the same shade, trading one contrast problem for another. Added a genuinely new `--bar-surface` (`#2a2930` dark, `#bfb9a5` light) instead.
- [x] T009 [artifacts: brand] Revise `brand.md`'s palette table: add a `--bar-surface` row alongside the existing `--surface` row (both dark and light value tables), with a one-line rationale (bar needs to read as distinct from the cards/panels it shares `--surface` with). Bump `last_updated`.
- [x] T010 Manual verification in dark mode: confirm the persistent Bar now visually reads as distinct from surrounding Lobby/Playback content, and that the hazard-stripe fill on top of it (unchanged, per the plan's declined-reversal scope) still renders correctly over the new base color.

  **Confirmed via real browser**, both via computed-style inspection (`.bar`'s `background-color` resolves to `rgb(42, 41, 48)` — exactly `--bar-surface`) and visually (Bar reads as a distinct lighter panel against the near-black canvas, hazard-stripe tape renders correctly on top unchanged). Aside: the lyrics overlay (Phase 2) also renders during this check since it's not gated to the Playback view — this matches the app's existing pre-playback tab-preview design (visible once a part is selected, `hasPart`-gated not view-gated), not a new interaction introduced by this phase.

## Phase 4: Song/part-selection modal

- [ ] T011 [artifacts: ui] Build a new modal component (e.g. `client/src/components/SongPartModal.svelte`) reusing existing `ListRow`/`Button` components, containing exactly the catalog list + part list content currently rendered inline at the top of `client/src/views/Lobby.svelte` (song selection, "Your part" list including the Lyrics row) — read `Lobby.svelte` first to lift this logic faithfully rather than re-deriving it, including all existing host-only/disabled-state conditionals.
- [ ] T012 Add a nav-bar (persistent Bar) control to open/close the modal, and wire auto-open logic: the modal opens automatically whenever `!session.selectedSong` or the current participant's `selectedPart` is `null`, checked wherever `App.svelte` already derives comparable reactive state (its existing `$: hasPart` pattern is the precedent). Remove the now-redundant inline catalog/part-list rendering from `Lobby.svelte`, keeping the participant list, lobby cursor, and Spotlight-mode controls inline as before.
- [ ] T013 Write a test first (Principle VII): add `client/e2e/song-part-modal.spec.ts` asserting (a) a freshly created session auto-opens the modal (no song/part selected yet); (b) selecting a song and a part allows the modal to be closed manually; (c) the nav-bar control reopens it afterward. Use the same `catalog/test-fixtures/fixture-catalog` synthetic song and locator patterns established in `client/e2e/single-participant.spec.ts`. Confirm this test fails against the current inline-Lobby-rendering code before T011/T012 are implemented (or, if written after, that it passes immediately reflecting the new modal — either order is acceptable, but it must be run and confirmed against real code, not merely written).
- [ ] T014 [artifacts: ui] Revise `ui.md`'s Lobby View section to describe the modal (trigger location, auto-open condition) in place of the current inline-rendering description. Bump `last_updated` and set `diagram_status: stale` if not already.

## Phase 5: Full suite verification

- [ ] T015 Run `pnpm --filter client test`, `pnpm --filter client test:ct`, and `pnpm --filter client test:e2e` together and confirm every test from Phases 1-4 passes alongside the existing suite (25 vitest / 10+ CT / 6+ E2E from `test-coverage-backfill` and `playwright-client-coverage`, plus this plan's additions), with no regressions in previously-passing tests. Report the final total test/file counts.
