---
plan: plan-theme-persistence-2026-07-03.md
generated: 2026-07-03
status: completed
---

# Tasks

## Phase 1: Fix + regression test

- [x] T001 [artifacts: infrastructure] Write a test first (Principle VII): extend `client/src/playback-engine.ct.spec.ts`. New test: use `page.addInitScript(() => { document.documentElement.dataset.theme = 'light'; })` before `mount(PlaybackEngineHarness, ...)` (so the attribute is set before `PlaybackEngineHarness.svelte`'s `onMount` calls `ensurePlaybackEngine`, matching how `tab-renderer.ct.spec.ts`'s existing `setTheme` test drives theme-dependent color assertions). After mounting, assert the rendered tab's colors match `lightTabColors` (`client/src/brand-colors.ts`) rather than the `darkTabColors` default — e.g. `component.getByTestId('tab-container').locator('svg text').first().evaluate((el) => getComputedStyle(el).fill)` compared against `lightTabColors`'s corresponding value, mirroring `tab-renderer.ct.spec.ts`'s "setTheme visibly changes rendered resource colors" test's assertion style. Confirm this fails against the current code (today it renders in dark colors regardless of `data-theme`, since `ensurePlaybackEngine` hardcodes `theme: 'dark'`).

  Added `a freshly-created engine picks up the document theme instead of hardcoding dark` to `playback-engine.ct.spec.ts`. Resolved `lightTabColors.foreground` to a canonical `getComputedStyle`-format string by round-tripping the rgba values through a throwaway DOM element's `style.color`, rather than hand-formatting an `rgb(...)` literal (no precedent for that in this repo, and it avoids an assumption about alphaTab's exact fill-serialization format).

  **Deviation from the task's literal instruction, caught during red confirmation:** `page.addInitScript()` as originally specified does *not* work here — Playwright CT's `page` fixture has already navigated to the mount index page before the test body runs (confirmed via debug logging: `page.url()` was already `http://localhost:3100/` before `addInitScript` was even registered), so an init script registered inside the test body only affects *future* navigations, never the one that already happened. The attribute was silently never set (`dataset.theme` read back as `undefined`), which meant the first "red" run was coincidentally red for the wrong reason — the assertion never actually got a `'light'`-themed document at all, dark-hardcoded or not. Switched to `await page.evaluate(() => { document.documentElement.dataset.theme = 'light'; })` before `mount()` instead (sets the attribute directly on the already-loaded document). Re-confirmed true red by stashing the (at-that-point-unwritten) fix — n/a here since T002 hadn't been written yet, confirmed instead by running this corrected test against the still-hardcoded `theme: 'dark'` code: genuinely red with `rgb(255, 230, 0)` (dark's foreground) instead of the expected `rgb(138, 106, 0)` (light's foreground) — for the right reason this time.

- [x] T002 [artifacts: infrastructure] Fix `ensurePlaybackEngine()` in `client/src/playback-engine.ts`: at the top of the function, compute `const theme: Theme = document.documentElement.dataset.theme === 'light' ? 'light' : 'dark';` (mirrors `main.ts`'s `loadStoredTheme() ?? 'dark'` fallback — both agree dark is the default absent any preference). Use this `theme` value for `state.theme`'s initial value (replacing the current hardcoded `'dark'` literal at `state = { api, isLyricsPart, theme: 'dark', ... }`) and pass it as `createTabRenderer()`'s `theme` option (currently only ever called with its default). Run T001's test and confirm it passes.

  Implemented exactly as specified. T001's test now passes; full `test:ct` suite (21 tests, 12 spec files) passes with no regressions.

- [x] T003 [artifacts: ui] No `ui.md` content change is needed — confirm this rather than skip it silently: `ui.md` never documented the buggy hardcoded-dark behavior in the first place (it already correctly states theme toggling "switches both the app's CSS palette and the tab notation's colors together"), so this is a pure implementation-bug fix bringing code in line with the artifact's existing, already-correct claim — not a design reversal requiring an artifact edit.

  Confirmed: `ui.md` read in full, contains no reference to the buggy hardcoded-dark behavior. No edit made.

## Phase 2: Manual verification

- [x] T004 Manual verification in a real browser: with dev servers running (note: if starting the server manually rather than via `pnpm --filter server dev` from the repo root, set `CATALOG_ROOT` to the repo root's `catalog/` absolute path, or run from the `server/` directory — the default `./catalog` is relative to cwd and silently resolves to an empty catalog with no error if run from the wrong directory, as found during this session's own live-browser pass). Toggle to light mode via the Settings modal's Settings tab, select an instrument part, refresh the page, and confirm the tab notation now loads directly in light-mode colors (no longer reverting to dark and requiring a manual re-toggle, per this plan's fix). Also confirm dark mode (the default, nothing persisted — e.g. after clearing `localStorage`) still renders correctly unchanged.

  **No real browser available in this session — verified by code reading and the automated CT test instead, real live-browser confirmation is still pending human verification.** Traced the actual runtime path: `main.ts` calls `applyTheme(loadStoredTheme() ?? 'dark')` at startup, which sets `document.documentElement.dataset.theme` before any song/part is selected; `ensurePlaybackEngine()` (only ever called later, once a part is selected) now reads that same attribute via the T002 fix. T001's CT test exercises exactly this sequence end-to-end against a real alphaTab render (attribute set to `'light'` before the engine is constructed, real SVG output asserted to carry `lightTabColors`, not `darkTabColors`) and passes. Dark-mode-unchanged path: reasoned, not separately tested — the fallback `document.documentElement.dataset.theme === 'light' ? 'light' : 'dark'` degrades to `'dark'` for any value other than `'light'` (including `undefined`, matching a cleared-`localStorage` first visit), which is exactly the pre-existing default behavior all of `playback-engine.ct.spec.ts`'s other tests already implicitly rely on (they mount with no theme attribute set and were passing before this change too). A human should still confirm both scenarios live in a real browser per this task's original instruction — not claiming visual confirmation that wasn't actually possible in this session.

## Phase 3: Full suite verification

- [x] T005 Run `pnpm --filter client test`, `pnpm --filter client test:ct`, and `pnpm --filter client test:e2e`. Confirm every test passes with no regressions. Report final test/file counts.

  **All green, no regressions.** vitest: 6 files / 25 tests. CT: 8 files / 21 tests — this plan added exactly 1 new test (`playback-engine.ct.spec.ts`'s theme-persistence regression test); the file count differs from `tasks-ui-polish-pass-b013.md`'s last-reported "12 files" figure, but that's pre-existing drift from intervening work on other branches (e.g. `song-catalog-selection`, merged since), not something this plan's changes caused — confirmed via `find . -name "*.ct.spec.ts"` that exactly 8 CT spec files exist on disk today. E2E: 4 files / 10 tests (this plan touched no E2E files, count unaffected). Total across `client`: 18 files / 56 tests.
