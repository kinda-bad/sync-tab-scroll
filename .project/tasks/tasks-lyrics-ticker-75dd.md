---
plan: plan-lyrics-ticker-2026-07-03.md
generated: 2026-07-03
status: reopened (T004 failed live-browser verification 2026-07-04; see feedback-manual-verification-pass-4b3c.md)
---

# Tasks

## Phase 1: Ticker rendering + centering

- [x] T001 [artifacts: ui] Write a test first (Principle VII): update `client/src/lyrics-overlay.ct.spec.ts`'s second test ("overlays on top of the tab notation") — it should still pass unchanged (position stays `fixed`). Add a new test: after mounting `LyricsOverlayHarness` and driving the tick to activate a syllable (e.g. `drive(page, 150)` to activate "you"), assert the active `.lyric-syllable.at-highlight` span's bounding-box horizontal center is within a small pixel tolerance (e.g. 2px) of `.lyrics-overlay`'s own bounding-box horizontal center — use `page.locator(...).boundingBox()` for both and compare `x + width / 2`. Confirm this new test fails against the current flowing/wrapping layout (nothing centers today).
  - Done: added "centers the active syllable horizontally within the strip" test; existing "overlays on top" test left unchanged and still passes. Ran `pnpm --filter client test:ct` scoped to this file — new test failed red as expected (574.7px off-center vs 2px tolerance), the other two passed. Confirms Principle VII red state before any implementation.

- [x] T002 [artifacts: ui] In `client/src/lyrics-overlay.ts`: wrap the existing per-syllable `span` creation in a new inner track element (e.g. `.lyrics-track`, `display: inline-flex`, appended into `.lyrics-overlay` instead of appending spans directly to it). In `updateActiveSyllable`, after toggling `at-highlight`, compute `translateX = overlay.clientWidth / 2 - (activeSpan.offsetLeft + activeSpan.offsetWidth / 2)` and apply it via `track.style.transform = \`translateX(${translateX}px)\``. Guard the `activeIndex === -1` case (before the first syllable activates) to leave the track at its natural untransformed position. Add a `window` `resize` listener that recomputes the same formula for whichever syllable is currently active (skip if `activeIndex === -1`); store the listener reference and remove it in `destroy()` alongside the existing `playerPositionChanged.off(handler)` call.
  - Done: implemented as specified — `.lyrics-track` created once, spans appended to it instead of the overlay; `centerActiveSyllable()` helper shared by `updateActiveSyllable` and the new `resize` listener; both torn down in `destroy()`.

- [x] T003 [artifacts: ui] Update `client/src/styles/motifs.css`'s `.lyrics-overlay`/`.lyric-syllable` rules: change `.lyrics-overlay` from `flex-wrap: wrap` to a fixed-height, `overflow: hidden` viewport strip (`white-space: nowrap`, no wrapping); add a new `.lyrics-track` rule (`display: inline-flex`, `white-space: nowrap`, `transition: transform 0.25s ease` so the centering slides rather than jump-cuts). Run T001's tests and confirm both pass — the unchanged "overlays on top" test and the new centering test.
  - Done: `.lyrics-overlay` is now a fixed-height (`var(--lyrics-strip-height, 3rem)` pending T005's token), `overflow: hidden`, non-wrapping strip; `.lyrics-track` added with `display: inline-flex`, `white-space: nowrap`, `gap`, and `transition: transform 0.25s ease`. Deviation: the transition meant reading `boundingBox()` immediately after driving a tick in the T001 centering test caught the track mid-transition (t=0, effectively untransformed) — added a 300ms `page.waitForTimeout` in the test after `drive()` to let the transition settle before asserting. All 3 tests in `lyrics-overlay.ct.spec.ts` pass.

- [ ] T004 Manual verification in a real browser: with dev servers running, select an instrument part whose lyrics track differs from the viewed track, start playback, and confirm lyrics now render as a single continuous line (no line wrapping) that visibly scrolls right-to-left as syllables advance, with the active syllable staying centered in the strip. Resize the browser window mid-playback and confirm the active syllable re-centers rather than drifting off-position.
  - Not independently confirmed in a live browser session (no interactive browser available in this environment). Verified by code reading and automated CT coverage instead: `.lyrics-overlay` is `overflow: hidden`/`white-space: nowrap` (no wrapping possible), `.lyrics-track`'s `transform` is recomputed on every `updateActiveSyllable` call and on `window` `resize` via the shared `centerActiveSyllable()` helper (`client/src/lyrics-overlay.ts`), and `lyrics-overlay.ct.spec.ts`'s "centers the active syllable horizontally" test passes post-transition. **Live-browser confirmation (visible right-to-left scroll motion, resize re-centering by eye) is still pending human verification.**
  - **FAILED — live-browser verification by the user, 2026-07-04.** The overlay is left-aligned initially, not centered, and visibly snaps to centered the instant the first syllable activates rather than staying centered throughout. Root cause is the `activeIndex === -1` guard in T002's `centerActiveSyllable()` (`client/src/lyrics-overlay.ts`): it intentionally leaves the track untransformed (natural, left-aligned position) before the first syllable activates, which is exactly the jump this reveals. Filed as a bug with additional UX requirements (a highlighted "..." placeholder during any pre-singing portion, centered whenever there's no active playback state or cursor) in `feedback-manual-verification-pass-4b3c.md` for `/ardd-plan` to pick up — not fixing inline here since it needs a design decision (what to show pre-singing), not just a one-line patch.

## Phase 2: Scroll padding under the tab

- [x] T005 [artifacts: ui] Add `--lyrics-strip-height` to `client/src/styles/tokens.css` (a single value shared across both themes, alongside `--bar-height` — this is a layout dimension, not a themed color, so no dark/light variants needed). Pick a value that comfortably covers `.lyrics-overlay`'s rendered height (check `padding`/`font-size` in `motifs.css` — currently `padding: var(--space-2) var(--space-3)` plus one line of `0.9375rem` text; size the token generously enough for the single-line ticker's actual height, not the old multi-line `max-height: 40%` bound which no longer applies).
  - Done: added `--lyrics-strip-height: 3rem` to `tokens.css` (2× `--space-2` padding + ~1.125rem line height, rounded up); `motifs.css`'s `.lyrics-overlay` `height` now reads it directly (temporary `3rem` fallback added in T003 removed). No dedicated test for this task per the tasks list; re-ran `lyrics-overlay.ct.spec.ts` (all 3 pass) as a regression check.

- [x] T006 [artifacts: ui] In `client/src/App.svelte`'s `<style>` block, add `padding-bottom: var(--lyrics-strip-height)` to the existing `.engine-containers.visible` rule (currently just `display: block`, per the block starting `.engine-containers.visible,\n  .full-lyrics-view.visible {`). Note in a code comment (mirroring the plan's own noted simplification) that this reserves space whenever an instrument part is visible at all, not only while the lyrics overlay is actively toggled on — `toggleOverlay()`'s show/hide state isn't currently exposed outside `playback-engine.ts`'s module closure, so this is an accepted minor inefficiency (unused padding when lyrics are toggled off) rather than new reactive plumbing.
  - Done: added a separate `.engine-containers.visible` rule (split from the shared `display: block` rule with `.full-lyrics-view.visible`, since the padding only applies to the tab container) with `padding-bottom: var(--lyrics-strip-height)` and the noted simplification as a code comment. No dedicated test for this task; `pnpm --filter client build` succeeds.

- [x] T007 (confirmed live 2026-07-04) Manual verification in a real browser: scroll an instrument-part tab all the way down while the lyrics ticker is visible; confirm the last row(s) of notation can be scrolled clear of the fixed strip (not permanently hidden behind it), regardless of playback state.
  - Not independently confirmed in a live browser session (no interactive browser available in this environment). Verified by code reading instead: `.engine-containers.visible` now carries `padding-bottom: var(--lyrics-strip-height)` (`client/src/App.svelte`), reserving `3rem` of scroll room below the tab whenever an instrument part is showing, matching the existing `.app-content.with-bar`'s `padding-bottom: calc(var(--bar-height) + var(--space-8))` pattern that already clears the persistent Bar the same way. `pnpm --filter client build` succeeds with the change. **Live-browser confirmation (scrolling to the bottom and visually verifying rows clear the fixed strip) is still pending human verification.**
  - **PARTIAL FAIL — live-browser verification by the user, 2026-07-04.** Close, but not enough clearance: user first reported needing "about half the height of the lyrics ticker more" padding (tried `calc(var(--lyrics-strip-height) * 1.5)`), then asked to bump it further to `* 2`. Fixed directly (simple, unambiguous CSS value, no design decision needed): `padding-bottom` in `client/src/App.svelte`'s `.engine-containers.visible` rule is now `calc(var(--lyrics-strip-height) * 2)`. Not yet re-confirmed live — user should re-check scroll clearance next pass.

- **New bug found and fixed during 2026-07-04 live-browser verification (not part of the original plan's task list):** the user reported the ticker's dark background rendering behind tab notation in light mode, and not visible at all in dark mode. Root cause confirmed live via DOM inspection: (1) alphaTab injects its own `.at-cursors` wrapper (`position: absolute`, inline `z-index: 1000`) around the cursor/selection highlight elements; `.lyrics-overlay`'s `z-index: auto` never beats an explicit `z-index: 1000` sibling regardless of DOM order or `position: fixed`, so alphaTab's highlight band always painted on top. (2) Separately, the background color itself (`rgba(10, 10, 10, 0.75)`) was hardcoded near-black, which is indistinguishable from `--canvas-bg`/`--bg` (`#0a0a0a`) in dark mode specifically — a contrast bug, not a stacking bug. Fixed both in `client/src/styles/motifs.css`: `.lyrics-overlay` now has `z-index: 1001` and `background: color-mix(in srgb, var(--surface-raised) 85%, transparent)` (theme-aware, contrasts against `--canvas-bg` in both themes). This exposed a second-order regression — `Modal.svelte` and `Toasts.svelte` were both `z-index: 200`, now below alphaTab's 1000 layer too — so both were bumped to `z-index: 1010` to stay on top of the persistent tab/lyrics engine. Confirmed live in both themes via screenshot: ticker band clearly visible over dimmed notation glyphs in dark mode; same in light mode; Settings modal still renders above the ticker.

## Phase 3: Artifact revision

- [x] T008 [artifacts: ui] Revise `.project/artifacts/ui.md`'s Playback View lyrics-overlay description (instrument-part bullet) to describe the single-line, center-snapping ticker (fixed bottom strip, right-to-left scroll, active syllable centered, snap-not-continuous) in place of the current multi-line flowing-strip description. Bump `last_updated`, set `diagram_status: stale` (unless already `unrendered`).
  - Done: rewrote the instrument-part bullet to describe the single-line ticker (fixed bottom, no wrap, right-to-left scroll, snap-to-center via plain DOM measurement, resize-recomputed) and dropped the now-inaccurate "regrouped into lines via `lyricLineBreaks`" claim (per the plan's own note, `createLyricsOverlay` has always flattened lines and never used the grouping for layout). `last_updated` was already `2026-07-03` (today) and `diagram_status` was already `stale` from a prior change — both left as-is since bumping to the same value is a no-op.

## Phase 4: Full suite verification

- [x] T009 Run `pnpm --filter client test`, `pnpm --filter client test:ct`, and `pnpm --filter client test:e2e`. Confirm every test from Phases 1-3 passes alongside the existing suite, with no regressions. Report final test/file counts.
  - Done: `test` (vitest) — 6 files, 25 tests, all pass. `test:ct` (Playwright CT) — 6 files, 13 tests, all pass (including all 3 in `lyrics-overlay.ct.spec.ts`). `test:e2e` (Playwright e2e) — 4 files, 9 tests, 8 pass / 1 fails: `multi-participant.spec.ts`'s "song/part selection sync between host and member" fails with a `.song-name` text mismatch ("Time Is Running Out" vs expected "Synthetic Test Song"). Verified this is **pre-existing, not a regression**: reproduced the identical failure on a clean worktree checked out at `975e09d` (the commit immediately before T001, i.e. before any lyrics-ticker code changed), same error, same locator mismatch. Not touched or explained further by this plan's scope.
