---
plan: plan-test-coverage-2026-07-02.md
generated: 2026-07-02
status: in-progress
---

# Tasks

## Phase 1: Client test infrastructure

- [x] T001 Add `vitest` as a `client` dev dependency, pinned to the same `^2.1.9` used in `server/package.json` (avoid version drift between the two packages: run `pnpm --filter client add -D vitest@^2.1.9`). Add a `"test": "vitest run"` script to `client/package.json`, matching `server/package.json`'s existing script. Confirm `pnpm --filter client test` runs (even with zero test files, vitest should report "no test files found" rather than erroring) before moving on.

## Phase 2: Client pure-logic test backfill

- [ ] T002 [parallel] Write `client/src/lrc-parser.test.ts` covering `parseLrc` (`client/src/lrc-parser.ts`): a normal timestamped line (`[00:12.34]some lyric`) parses to `{ timeMs: 12340, text: 'some lyric' }`; a blank "gap" line (`[00:15.00]`, empty text after the bracket) parses to `{ timeMs: 15000, text: '' }`; a line not matching the `[MM:SS.ss]` bracket pattern at all is skipped, not thrown; multi-line input preserves order. Run `pnpm --filter client test` and confirm it passes.
- [ ] T003 [parallel] Write `client/src/lyrics-beat-walk.test.ts` covering both exports of `client/src/lyrics-beat-walk.ts`. For `walkLyricBeats`: build a minimal fake `at.model.Score`-shaped object (plain object literal, cast `as any` — `{ tracks: [{ staves: [{ bars: [{ masterBar: { start: N }, voices: [{ beats: [{ lyrics: [...], playbackStart: N }] }] }] }] }] }`) with (a) one beat per syllable — confirm each syllable's `tickPosition` is `bar.masterBar.start + beat.playbackStart`; (b) consecutive beats sharing identical lyric text — confirm they collapse into one syllable, keeping the first beat's tick (the melisma case documented in the file's own comment); (c) `lyricsLineIndex` selecting a specific lyric channel when a beat's `lyrics` array has multiple entries. For `groupIntoLines`: a flat syllable array regrouped by a `lyricLineBreaks` count array produces lines with the right per-line syllable counts, cursor advancing correctly across lines. Run `pnpm --filter client test` and confirm it passes.
- [ ] T004 [parallel] Write `client/src/toast-store.test.ts` covering `client/src/toast-store.ts`'s `toastStore`: `push(message)` appends a `{ id, message }` toast to the store (assert via `get(toastStore)` or a manual `subscribe` call) with a monotonically incrementing `id` across multiple pushes; the toast is auto-removed from the store 5000ms later (use `vi.useFakeTimers()` + `vi.advanceTimersByTime(5000)`, confirm the toast is gone from the store's array afterward). Run `pnpm --filter client test` and confirm it passes.
- [ ] T005 [parallel] Write `client/src/session-persistence.test.ts` covering `client/src/session-persistence.ts`. Stub `globalThis.localStorage` with a minimal in-memory `Map`-backed object (`getItem`/`setItem`) for the test file (no `jsdom` — Principle V, this project's existing idiom per the plan). For `startSessionPersistence`: subscribing to a fake `clientStore`-shaped store (or exercising the real `clientStore` from `./store` by setting its state, whichever is less invasive to fake) confirms nothing is written to `localStorage` when `session`/`selfParticipantId` are absent, and a `{code, displayName, participantId}` object is written once both are present and the participant is found in `session.participants`. For `loadStoredSession`: returns `undefined` when nothing is stored; round-trips a previously-stored `StoredSession` object exactly. Run `pnpm --filter client test` and confirm it passes.
- [ ] T006 [parallel] Write `client/src/readiness.test.ts` covering `waitUntilReady` (`client/src/readiness.ts`). Build a fake `AlphaTabApi`-shaped object with `scoreLoaded`/`soundFontLoaded` each shaped as `{ on: (cb) => { capturedCallback = cb; } }` so the test can manually invoke the captured callback to simulate each event firing. Confirm the returned promise does not resolve after only one of the two events fires, and does resolve once both have fired — test both firing orders (`scoreLoaded` then `soundFontLoaded`, and the reverse) to confirm order doesn't matter. Run `pnpm --filter client test` and confirm it passes.
- [ ] T007 [parallel] Write `client/src/playback-sync.test.ts` covering both exports of `client/src/playback-sync.ts`. For `correctDrift`: (a) returns `null` immediately if `api.isReadyForPlayback` is false, without touching `api.tickPosition`; (b) `playbackState.status === 'running'` and `api.playerState` not `Playing` — sets `api.tickPosition` to `playbackState.tickPosition`, calls `api.play()`, and returns that tick; (c) `playbackState.status !== 'running'` and `api.playerState === Playing` — calls `api.pause()`; (d) drift between `api.tickPosition` and `playbackState.tickPosition` exceeding `DRIFT_THRESHOLD_TICKS` (50) — snaps `api.tickPosition` and returns the applied tick; (e) drift within the threshold — returns `null` and leaves `api.tickPosition` unchanged. Build a fake `AlphaTabApi`-shaped object with a plain mutable `tickPosition`/`playerState`/`isReadyForPlayback` and spy `play`/`pause` functions (`vi.fn()`), and a fake `PlaybackState` object. For `applyPlaybackSettings`: confirms `api.metronomeVolume`/`api.countInVolume` are set to `1`/`0` based on `session.metronomeEnabled`/`countInEnabled`. Run `pnpm --filter client test` and confirm it passes.

## Phase 3: Server module test backfill

- [ ] T008 [parallel] Write `server/src/session-store.test.ts` covering `server/src/session-store.ts`'s `SessionStore`: `create(hostId)` returns a `Session` with a 4-character `code` drawn from the documented charset, `hostId` set, and all documented defaults including `spotlightMode: false`, `lobbyCursorTick: null`, `countInEnabled: false`, `metronomeEnabled: false`; two `create()` calls never collide on `code` (can't test the collision-retry path directly without mocking `Math.random`, so just assert distinctness across many calls). `markPossiblyEmpty` schedules destruction (use `vi.useFakeTimers()`) only when no participants are `connected`; `markActive` cancels a pending destruction timer. `scheduleHostReassignment` is a no-op if already pending (calling it twice only schedules one timer — verify via `vi.getTimerCount()` or by confirming the callback only fires once after advancing time); `cancelHostReassignment` clears a pending timer so its callback never fires. Run `pnpm --filter server test` and confirm it passes.
- [ ] T009 [parallel] Write `server/src/connections.test.ts` covering `server/src/connections.ts`'s `ConnectionRegistry`: `attach` + `get` round-trips a socket's `ConnectionInfo`; `detach` removes it and returns the removed info (or `undefined` if the socket wasn't attached); `send` calls the given fake socket's `send` with the JSON-stringified message; `broadcast(sessionCode, buildMessage)` calls `send` on every socket attached under that session code (and none from a different session code), passing each socket's own `participantId` into `buildMessage`. Use fake `WebSocket`-shaped objects (`{ send: vi.fn() } as unknown as WebSocket`). Run `pnpm --filter server test` and confirm it passes.
- [ ] T010 [parallel] Write `server/src/host-succession.test.ts` covering `promoteNextHost` (`server/src/host-succession.ts`): no-op (no broadcast, no state change) if `ctx.sessionStore.get(code)` returns undefined; no-op if the current host's `connectionStatus` is still `'connected'`; no-op if there are no other `connected` participants to promote; otherwise promotes the longest-tenured (lowest `joinedAt`) connected non-host participant — confirm the old host's `role` flips to `'member'`, the new host's `role` flips to `'host'`, `session.hostId` updates, and a `session-state` broadcast fires. Build a real `SessionStore`/`ConnectionRegistry` plus a manually-constructed `Session` object (or one from `sessionStore.create()` with participants pushed onto it directly) for each scenario. Run `pnpm --filter server test` and confirm it passes.
- [ ] T011 [parallel] Write `server/src/catalog-loader.test.ts` covering `loadCatalog` (`server/src/catalog-loader.ts`) against real temp-directory fixtures built with `fs.mkdtempSync(path.join(os.tmpdir(), 'catalog-'))` in a `beforeEach`/cleaned up in `afterEach`: a well-formed song directory (valid `meta.json`, a `.gp` file present) produces a `CatalogSong` with the right `id` (matching the directory name), `gpFilePath`/`lyricsLrc` URL prefixes, and passed-through `meta.json` fields; a directory missing `meta.json` or with no `.gp` file is skipped (not thrown, and doesn't appear in the returned array — confirm via a fixture with two directories, one broken and one valid, only the valid one comes back); a directory with `lyrics.lrc` present vs. absent toggles `lyricsLrc` between a path and `null`; a nonexistent `catalogRoot` path returns `[]`. Run `pnpm --filter server test` and confirm it passes.
- [ ] T012 [parallel] Write `server/src/catalog-static.test.ts` covering `createCatalogRequestHandler` (`server/src/catalog-static.ts`) against a real temp-directory fixture: a request for an existing file under `/catalog/<relative-path>` responds 200 with the right `Content-Type` per extension (`.gp` → `application/octet-stream`, `.lrc` → `text/plain; charset=utf-8`) and streams the file content; a request for a nonexistent file 404s; a path-traversal attempt (`/catalog/../../etc/passwd`-style) 404s rather than escaping `catalogRoot` (verify by asserting the response is a 404, not that a file outside the fixture dir was ever read); a URL not starting with `/catalog/` returns `false` from the handler function itself (indicating "not handled") without writing any response. Build minimal fake `IncomingMessage`/`ServerResponse`-shaped objects (`{ url } as IncomingMessage`, and a `ServerResponse` fake capturing `writeHead`/`end` calls, or a real `http.ServerResponse` if simpler to construct in-test). Run `pnpm --filter server test` and confirm it passes.
- [ ] T013 [parallel] Write `server/src/config.test.ts` covering `loadConfig` (`server/src/config.ts`): with `PORT`, `CATALOG_ROOT`, and `HOST_REASSIGN_GRACE_MS` all set in `process.env`, `loadConfig()` returns those exact values (numbers coerced correctly); with none set, returns the documented defaults (`8080`, `'./catalog'`, `120000`). Save/restore `process.env` around each test (or use `vi.stubEnv`) so this test doesn't leak env state into other test files. Run `pnpm --filter server test` and confirm it passes.
- [ ] T014 [parallel] Write `server/src/handlers/session-create.test.ts` covering `handleSessionCreate`: creates a new session via `sessionStore`, attaches the requesting socket as host, and sends/broadcasts the resulting `session-state`. Follow the fake-socket/real-store pattern from `spotlight-mode-set.test.ts`. Run `pnpm --filter server test` and confirm it passes.
- [ ] T015 [parallel] Write `server/src/handlers/session-join.test.ts` covering `handleSessionJoin`: joining an existing session by code adds/reconnects a participant and broadcasts `session-state`; joining a nonexistent code sends an `error` message instead. Cover the reconnect-by-`participantId` path (an existing participant id reattaches rather than minting a new participant) if present in the handler. Run `pnpm --filter server test` and confirm it passes.
- [ ] T016 [parallel] Write `server/src/handlers/part-select.test.ts` covering `handlePartSelect`: sets the requesting participant's `selectedPart` and broadcasts `session-state`. Run `pnpm --filter server test` and confirm it passes.
- [ ] T017 [parallel] Write `server/src/handlers/readiness-update.test.ts` covering `handleReadinessUpdate`: sets the requesting participant's `readiness` and broadcasts `session-state`. Run `pnpm --filter server test` and confirm it passes.
- [ ] T018 [parallel] Write `server/src/handlers/host-remove-participant.test.ts` covering `handleHostRemoveParticipant`: host-only check (non-host sender gets an `error`, no state change — same pattern as `spotlight-mode-set.test.ts`); host removing a participant updates `session.participants` and broadcasts `session-state`. Run `pnpm --filter server test` and confirm it passes.
- [ ] T019 [parallel] Write `server/src/handlers/playback-control.test.ts` covering `handlePlaybackControl`'s full `switch` over all five actions: host-only check; `start` sets `status: 'running'` and resets both `lobbyCursorTick` to `null` and `spotlightMode` to `false` (this is the one case already partially covered by `spotlight-mode-set.test.ts`'s third test — write the dedicated case here too, for completeness of this handler's own test file); `resume` sets `status: 'running'` without touching `lobbyCursorTick`/`spotlightMode`; `pause` sets `status: 'paused'`; `stop` sets `status: 'stopped'` and `tickPosition: 0`; `seek` with a `tickPosition` updates `playbackState.tickPosition`, and `seek` with `tickPosition: undefined` leaves it unchanged. Every action updates `playbackState.serverTimestamp` and broadcasts `session-state`. Run `pnpm --filter server test` and confirm it passes.
- [ ] T020 [parallel] Write `server/src/handlers/lobby-cursor-set.test.ts` covering `handleLobbyCursorSet`: host-only check (non-host gets the `'Only the host can set the lobby cursor'` error, no state change); host setting a `tickPosition` (including `null` to clear it) updates `session.lobbyCursorTick` and broadcasts `session-state`. Run `pnpm --filter server test` and confirm it passes.
- [ ] T021 [parallel] Write `server/src/handlers/song-select.test.ts` covering `handleSongSelect`: sets `session.selectedSong` and `session.availableParts` from the matched catalog entry; resets every participant's `selectedPart` to `null` and `readiness` to `'no-part'` (per `ui.md`'s documented re-selection behavior); broadcasts `session-state`. If the handler is host-only, cover that check too (read the handler first to confirm before writing the test, since `ui.md`/`features.md` don't explicitly state whether song-select is host-gated). Run `pnpm --filter server test` and confirm it passes.

## Phase 4: Full suite verification

- [ ] T022 Run `pnpm --filter client test` and `pnpm --filter server test` together (or `pnpm -r test` if that resolves cleanly across the workspace) and confirm every test file from Phases 2-3 passes with no failures, and no test leaks state into another (e.g. via shared `process.env`, fake timers not restored, or a shared `SessionStore` instance). Fix any such leakage found. Report the final total test/file count across both packages.
