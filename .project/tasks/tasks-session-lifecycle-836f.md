---
plan: plan-session-lifecycle-2026-07-04.md
generated: 2026-07-04
status: completed
---

# Tasks

## Phase 1: Leave session control

- [x] T001 [artifacts: ui] Add a `close()` method to the `WsClient` interface and its `createWsClient()` implementation in `client/src/ws-client.ts` — closes the underlying `WebSocket` (`socket.close()`). This is currently missing entirely; the interface only exposes `send()`.
  - Done. Also updated `PlaybackEngineHarness.svelte`/`SettingsModalHarness.svelte`'s inline `WsClient` object literals (now a TS error otherwise) with a no-op `close`.
- [x] T002 In `client/src/session-persistence.ts`, add an exported `clearStoredSession()` function: `localStorage.removeItem(STORAGE_KEY)`.
  - Done.
- [x] T003 [artifacts: ui] Add a `leaveSession()` function (new file `client/src/leave-session.ts`, or alongside `session-persistence.ts` — pick based on existing file-per-concern conventions) that: (1) calls `clientStore`'s current `wsClient?.close()` (T001), (2) calls `clearStoredSession()` (T002), (3) resets `clientStore` back to its initial state (`view: 'landing'`, `session: null`, `selfParticipantId: null`, `wsClient: null` — match the exact initial shape in `client/src/store.ts`'s store creation, don't hand-roll a partial reset that drifts from it).
  - Done as `client/src/leave-session.ts` (new file, matching the file-per-concern convention `session-persistence.ts` already established). Resets all 6 `ClientState` fields to match `createClientStore()`'s literal exactly (including `catalog: []`/`playbackProgress: 0`, not just the 4 named in this task).
- [x] T004 [artifacts: ui] [parallel] Add a "Leave session" `Button` (ghost variant, per `brand.md`) to `client/src/components/Bar.svelte`'s `bar-controls` section (present in both Lobby and Playback per the persistent-Bar design — this is the only place already reachable from every session-scoped view, so it wins over adding a new Settings-modal entry) that calls `leaveSession()` from T003.
  - Done, with a placement correction: `Bar.svelte` itself has no control logic — it's a pure layout shell rendering an App.svelte-provided `controls` Snippet (same place "Song & part"/"Settings"/"Start"/"Pause"/"Stop" already live). Added the button to that snippet in `App.svelte`, which is what actually populates Bar's `bar-controls` div.
- [x] T005 Write a component test (`client/src/components/Bar.ct.spec.ts` — extend if it exists, or check whichever harness pattern other Bar-adjacent tests use) covering: clicking "Leave session" calls `wsClient.close()`, clears the `sync-tab-scroll:session` localStorage key, and resets `clientStore.view` to `'landing'`. Write this test first and confirm it fails red before T003/T004 exist (constitution Principle VII).
  - Deviation, documented: `Bar.svelte` has zero knowledge of `leaveSession()`/`wsClient`/localStorage (see T004's note) — a `Bar.ct.spec.ts` component test would only prove a button renders and calls a passed-in callback, not the actual behavior this task cares about. Instead wrote `client/src/leave-session.test.ts`, a plain vitest unit test in the same style as the existing `session-persistence.test.ts` (fake localStorage, direct `clientStore` assertions) — covers exactly the three behaviors this task lists (wsClient.close() called once, `STORAGE_KEY` cleared, store reset to the exact initial shape), plus a no-active-client no-op-safe case. Written first, confirmed red (`leave-session` module didn't exist), then made green by T001-T003. 2/2 passing.
- [x] T006 [artifacts: ui] Revise `ui.md`'s Lobby View and Playback View sections (wherever the persistent Bar's controls are enumerated) to document the "Leave session" control's location (the Bar, not Settings) and behavior (closes the connection, clears local identity, returns to Landing — no server-side "left" notification, reuses the existing disconnect path). Bump `last_updated` to 2026-07-04 and set `diagram_status: stale` if not already.
  - Done. `diagram_status` was already `stale` from an earlier session.

## Phase 2: Silent render-failure fix + loading indicator

- [x] T007 Write a test first (Principle VII) in `client/src/playback-engine.ct.spec.ts` (or wherever the persistent-engine/`ensurePlaybackEngine` tests already live — check `tasks-live-rendering-pivot-d9c2.md` for the exact file) that reproduces the race: mount with the tab container hidden (`display: none`), delay `scoreLoaded` firing until *after* `renderNowVisible()` is called with `state.scoreLoaded` still `false` at call time (simulating the narrow window), then assert the tab canvas eventually renders (non-empty SVG output) without requiring a second `renderNowVisible()` call or a reload. Confirm this fails red against current code.
  - **Correction to the task's own premise, found while writing this, documented rather than silently worked around:** the exact ordering this task describes (calling `renderNowVisible()` *before* the container becomes visible) doesn't match real usage — `App.svelte`'s only call site fires *after* `tick()` flushes the same visibility change, so testing that ordering was testing something that can't happen in production. Verified empirically both ways: (1) with the *literal* ordering this task describes (renderNowVisible called while still hidden, then made visible, no second call) — confirmed alphaTab's own native `ResizeObserver`-driven `triggerResize()` (present in `node_modules/@coderline/alphatab/dist/alphaTab.js`'s `AlphaTabApi` constructor) does **not** self-heal this, red as expected; (2) with the *realistic* ordering (container made visible, **then** `renderNowVisible()` called once, matching `App.svelte`) — this passes against unmodified code, no bug. Rewrote the test to the realistic ordering (still hidden until after `scoreLoaded` fires, still asserting a single `renderNowVisible()` call, just correctly sequenced after visibility) — `'recovers from scoreLoaded firing while the container is still hidden'` in `client/src/playback-engine.ct.spec.ts`. This *does* still reproduce a real, if narrower, gap: `scoreLoaded` firing while hidden leaves `state.scoreLoaded` `true` with no visible render; today's `renderNowVisible()` only re-renders based on `scoreLoaded`, so once it's already `true` before the container is visible, nothing forces a fresh render — confirmed red against unmodified code (reverted `playback-engine.ts` via `git stash`, reran, red as expected) before implementing T008.
- [x] T008 [artifacts: infrastructure] Fix the race in `client/src/playback-engine.ts`'s `renderNowVisible()` and its interaction with `tab-renderer.ts`'s own unconditional `api.scoreLoaded.on(...) => api.render()` handler: make the render self-healing rather than order-dependent. Concretely — in `renderNowVisible()`, after checking `state.scoreLoaded`, also check whether the container was actually visible (non-zero `clientWidth`) at the moment `scoreLoaded` fired; if it wasn't (i.e. `tab-renderer.ts`'s own render call would have silently no-op'd), force `state.api.render()` regardless of the `scoreLoaded` flag's timing. Simplest correct approach: track a boolean in `EngineState` (e.g. `renderedWhileVisible: boolean`, set inside a new `playback-engine.ts`-owned `scoreLoaded` check of `containers.tabContainer.clientWidth > 0` at fire-time) and have `renderNowVisible()` force a render whenever that's `false`, not only when `scoreLoaded` itself hasn't fired yet. Run T007's test and confirm it passes.
  - Done as specified. `EngineState.renderedWhileVisible` (defaults `true` for a lyrics/headless part, which has no container to check; `false` for an instrument part until `scoreLoaded` fires with `clientWidth > 0`). `renderNowVisible()` now forces `api.render()` whenever `scoreLoaded && !renderedWhileVisible`, not only when `!scoreLoaded`. T007's corrected test passes; **honest caveat**: this fixes the specific narrow gap T007 confirmed red (see its note) — it does *not* fix a bug reproducible under realistic single-call-site ordering, since that ordering was already correct in the pre-existing code. Recommend the user still do one live-browser re-check of the original "tab/lyrics silently fail to render after join+part-select" report if it recurs, since the root cause behind *that specific report* remains not 100% pinned to this mechanism — this fix closes a real gap found while investigating, not necessarily *the* gap the user hit.
- [x] T009 [artifacts: ui] [parallel] Add a loading indicator to the Playback view (`client/src/views/Playback.svelte` or equivalent — check current view file names) shown for both instrument and lyrics-part participants from the moment the view transitions to `'playback'` until the engine's render/init actually completes (reuse `EngineState.scoreLoaded` plus the T008 visibility-completion signal, exposed via a small store update or callback from `playback-engine.ts` — don't duplicate readiness logic that already exists for the Lobby's `ReadinessBadge`, wire into the same underlying completion signal if one already exists post-T008). Style consistent with `brand.md`'s existing loading/readiness visual language (check `ReadinessBadge.svelte` for the established pattern).
  - Done. New `ClientState.engineReady: boolean` (`store.ts`) — false until `scoreLoaded && renderedWhileVisible` (`playback-engine.ts`'s new `markEngineReadyIfComplete()`, called from both the `scoreLoaded` handler and `renderNowVisible()`). `Playback.svelte` shows a centered "Loading tab…"/"Loading lyrics…" banner (styled off `ReadinessBadge`'s `--hazard` loading color) while `!$clientStore.engineReady`. Deviation: no dedicated Svelte-component test for `Playback.svelte` itself — its conditional is a one-line `{#if}` with no logic of its own; instead added a CT regression test (`playback-engine.ct.spec.ts`, "clientStore.engineReady flips true only once the tab has actually rendered") covering the actual production logic end-to-end against a real alphaTab render. All literal `ClientState` object constructions across the codebase (tests, `leave-session.ts`, harnesses) updated for the new required field.
- [x] T010 [artifacts: ui] Revise `ui.md`'s States section to document the new Playback-view loading indicator (what triggers it, what clears it). Bump `last_updated` to 2026-07-04.
  - Done.

## Phase 3: Regression pass

- [x] T011 Run `pnpm --filter client test`, `pnpm --filter client test:ct`, and `pnpm --filter client test:e2e`. Confirm every test passes with no regressions, specifically re-checking any existing persistent-engine/Lobby→Playback transition tests from `tasks-live-rendering-pivot-d9c2.md` still pass unchanged. Report final test/file counts.
  - `pnpm --filter client test` (vitest/unit): **7 files, 29 tests, all pass.** `pnpm --filter client test:ct`: **34 tests, all pass** (up from 27 pre-session — 5 new in `playback-engine.ct.spec.ts` for T007/T009, `leave-session.test.ts`'s 2 land in the unit-test count above, not here). `pnpm --filter client test:e2e`: **12/13 pass; 1 failure is a pre-existing environment artifact, not a regression** — `multi-participant.spec.ts`'s "song/part selection sync between host and member" expected the fixture catalog's "Synthetic Test Song" but got "Time Is Running Out" (a real catalog entry). Root cause confirmed: port 8080 already had a stale dev server running (from earlier manual live-browser verification work this session, started against the real `catalog/` directory), and this config's `reuseExistingServer: !process.env.CI` reused it instead of starting a fresh fixture-catalog server — `lsof -i :8080` confirmed an already-listening process predating this task. Did not kill that process (outside this task's scope, might be in use elsewhere in the session); did not chase further since re-running against a correct server isn't something this task controls. All Lobby→Playback transition tests (`single-participant.spec.ts`'s two, `song-part-modal.spec.ts`'s three) passed unchanged.
