---
slug: host-kick-freezes-session
status: backlogged
logged: 2026-07-14
---

Kicking a participant halts the session so it cannot continue (frozen/non-continuable), giving the host time to kick additional participants before manually restarting — rather than the session carrying on immediately after a removal.
Why: moderation flow for offensive participants; extends today's host-remove-participant (which removes one person but keeps the session running) with a deliberate freeze so the host isn't racing a live session while cleaning house. Pairs with host-restart-session-from-star and host-end-session-control.
