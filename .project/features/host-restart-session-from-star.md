---
slug: host-restart-session-from-star
status: backlogged
logged: 2026-07-14
---

The host can manually restart the session from the beginning after a freeze — resetting playback/state to the start (the user's 'restart the game from turn 0') so play resumes cleanly once unwanted participants are removed.
Why: the counterpart to host-kick-freezes-session — a frozen session needs an explicit host-driven restart to become playable again. NOTE: 'game'/'turn 0' is the user's framing; the current model is session + host-driven playback with no turn concept, so /ardd-plan must map 'turn 0' to the actual restart semantics (e.g. playback back to session start / lobby).
