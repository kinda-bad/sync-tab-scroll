node_modules/
dist/
.env

# Local test fixtures (real commercial .gp files — not for the repo)
client/public/test/

# Local catalog content (real commercial .gp files, pipeline output — not for the repo)
catalog/

# Claude Code local tooling/config — not for the repo
.claude/skills/ardd-*/
.claude/skills/shieldcn-badges/
skills-lock.json

*storybook.log
storybook-static

# Playwright CT build cache and test-run artifacts
client/playwright/.cache/
client/test-results/
client/playwright-report/
