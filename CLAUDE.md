# sync-tab-scroll

This project uses [artifact-driven-dev](https://github.com/[owner]/artifact-driven-dev).
See `.project/WORKFLOW.md` for the artifact list and skill commands, and
`.project/STATUS.md` for current artifact status and open questions.

## Commit messages

Use [Conventional Commits](https://www.conventionalcommits.org/) format:
`<type>(<optional scope>): <description>`, e.g. `feat(client): add
light-mode theme toggle` or `fix(server): reset lobbyCursorTick on
playback start`. Common types: `feat`, `fix`, `refactor`, `test`, `docs`,
`chore`.
