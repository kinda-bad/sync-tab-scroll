# Client config (constitution Principle VIII). Copy to .env and adjust
# locally — .env is git-ignored and never committed.
#
# VITE_BACKEND_PORT must stay equal to server/.env's PORT: both encode the
# dev backend port that the /catalog proxy and WS connection target.
VITE_BACKEND_PORT=6080
