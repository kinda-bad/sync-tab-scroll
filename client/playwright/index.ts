// Playwright component-test mount page entry point. No global setup
// needed — the modules under test (tab-renderer, playback-engine, etc.)
// don't depend on the punk-rock UI chrome tokens, only alphaTab/DOM/WS
// behavior.
